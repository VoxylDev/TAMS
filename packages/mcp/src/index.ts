/**
 * @tams/mcp — MCP server interface for the TAMS memory system.
 *
 * Exposes TAMS memory operations as MCP tools accessible by
 * any MCP-compatible client via STDIO transport.
 */

export { startServer } from './server.js';
